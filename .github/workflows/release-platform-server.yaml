name: Release platform-server

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write
  packages: write

env:
  IMAGE_NAME: ghcr.io/agynio/platform-server
  CHART_DIR: charts/platform-server
  HELM_REGISTRY: oci://ghcr.io/agynio/charts

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Extract version
        id: vars
        run: |
          if [[ "${GITHUB_REF_NAME}" != v* ]]; then
            echo "Tag must start with v" >&2
            exit 1
          fi
          VERSION="${GITHUB_REF_NAME#v}"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR (Docker)
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: packages/platform-server/Dockerfile
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:${{ steps.vars.outputs.version }}
            ${{ env.IMAGE_NAME }}:latest

      - name: Set up Helm
        uses: azure/setup-helm@v4

      - name: Log in to GHCR (Helm)
        run: |
          echo ${{ secrets.GITHUB_TOKEN }} | helm registry login ghcr.io --username ${{ github.actor }} --password-stdin

      - name: Build chart dependencies
        working-directory: ${{ env.CHART_DIR }}
        run: helm dependency build

      - name: Lint chart
        run: helm lint ${{ env.CHART_DIR }}

      - name: Package chart
        run: |
          mkdir -p dist
          helm package ${{ env.CHART_DIR }} \
            --destination dist \
            --version ${{ steps.vars.outputs.version }} \
            --app-version ${{ steps.vars.outputs.version }}

      - name: Push chart to GHCR
        run: |
          helm push dist/platform-server-${{ steps.vars.outputs.version }}.tgz ${{ env.HELM_REGISTRY }}
